import { ImageSourcePropType } from 'react-native';

/** Hero / banner illustrations used inside cards. */
export const HERO_IMAGES = {
  continue: require('./hero_continue.png') as ImageSourcePropType,
  myStory: require('./banner_mystory.png') as ImageSourcePropType,
  rewards: require('./hero_rewards.png') as ImageSourcePropType,
  celebrate: require('./scene_celebrate.png') as ImageSourcePropType,
  welcome: require('./hero_welcome.png') as ImageSourcePropType,
};
