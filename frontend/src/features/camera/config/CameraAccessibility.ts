export interface CameraAccessibilityOptions {
  muteVoiceGuidance: boolean;
  disableAnimations: boolean;
  slowCountdown: boolean;
  highContrast: boolean;
}

export const CameraAccessibility: CameraAccessibilityOptions = {
  muteVoiceGuidance: false,
  disableAnimations: false,
  slowCountdown: false,
  highContrast: false,
};
