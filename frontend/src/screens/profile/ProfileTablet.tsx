/**
 * My Profile, tablet layout — a wider reading column. See `ProfileContent`.
 */

import React from 'react';
import { ProfileContent } from './ProfileContent';

export const ProfileTablet: React.FC = () => <ProfileContent variant="tablet" />;

export default ProfileTablet;
