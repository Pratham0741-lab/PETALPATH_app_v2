import React from 'react';
import { ListenContent } from './ListenContent';

/**
 * Listen & Choose, desktop layout — a 320px mentor rail and the "Sound Lab"
 * heading over the play control. See `ListenContent` for the implementation
 * (spec §28).
 */
export const ListenDesktop: React.FC = () => <ListenContent variant="desktop" />;

export default ListenDesktop;
