import React from 'react';
import { ListenContent } from './ListenContent';

/**
 * Listen & Choose, tablet layout — the activity beside a 280px mentor rail.
 * See `ListenContent` for the implementation (spec §28).
 */
export const ListenTablet: React.FC = () => <ListenContent variant="tablet" />;

export default ListenTablet;
