import React from 'react';
import { HomeContent } from './HomeContent';

/** Desktop Home — same screen, wider capped content column. */
export const HomeDesktop: React.FC = () => <HomeContent variant="desktop" />;

export default HomeDesktop;
