import React from 'react';
import { HomeContent } from './HomeContent';

/** Tablet Home — same screen, wider capped content column. */
export const HomeTablet: React.FC = () => <HomeContent variant="tablet" />;

export default HomeTablet;
