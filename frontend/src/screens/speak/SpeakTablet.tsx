import React from 'react';
import { SpeakContent } from './SpeakContent';

/**
 * Speak & Learn, tablet layout — practice beside a 280px mentor rail.
 * See `SpeakContent` for the implementation (spec §28).
 */
export const SpeakTablet: React.FC = () => <SpeakContent variant="tablet" />;

export default SpeakTablet;
