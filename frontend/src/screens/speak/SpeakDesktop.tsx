import React from 'react';
import { SpeakContent } from './SpeakContent';

/**
 * Speak & Learn, desktop layout — a 320px mentor rail and the "Microphone
 * Studio" heading over the mic. See `SpeakContent` for the implementation
 * (spec §28).
 */
export const SpeakDesktop: React.FC = () => <SpeakContent variant="desktop" />;

export default SpeakDesktop;
