/**
 * My Rewards, tablet layout — a wider content column and a two-up collection
 * grid. See `RewardsContent`.
 */

import React from 'react';
import { RewardsContent } from './RewardsContent';

export const RewardsTablet: React.FC = () => <RewardsContent variant="tablet" />;

export default RewardsTablet;
