/**
 * My Rewards, desktop layout — the widest content column and a three-up
 * collection grid. See `RewardsContent`.
 */

import React from 'react';
import { RewardsContent } from './RewardsContent';

export const RewardsDesktop: React.FC = () => <RewardsContent variant="desktop" />;

export default RewardsDesktop;
