/**
 * Magical Garden, tablet layout — a wider column, a taller garden scene and a
 * two-up buddy grid. See `MentorContent`.
 */

import React from 'react';
import { MentorContent } from './MentorContent';

export const MentorTablet: React.FC = () => <MentorContent variant="tablet" />;

export default MentorTablet;
