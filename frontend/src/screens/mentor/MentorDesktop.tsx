/**
 * Magical Garden, desktop layout — the widest column, the largest garden scene
 * and a three-up buddy grid. See `MentorContent`.
 */

import React from 'react';
import { MentorContent } from './MentorContent';

export const MentorDesktop: React.FC = () => <MentorContent variant="desktop" />;

export default MentorDesktop;
