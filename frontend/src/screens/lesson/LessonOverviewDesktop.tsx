import React from 'react';
import { LessonOverviewContent } from './LessonOverviewContent';

/** Desktop Lesson Overview — same layout, a wider 320px status rail. */
export const LessonOverviewDesktop: React.FC = () => <LessonOverviewContent variant="desktop" />;

export default LessonOverviewDesktop;
